# Run Tests

Run the test suite for this project. Execute:

```bash
cd /Users/mac/Documents/One-pay && .venv/bin/python -m pytest tests/ -v --tb=short
```

After tests complete, summarize:
- Total tests run
- Pass/fail count
- Any test failures (show the failure messages)
- Test duration

If tests fail, investigate the root cause and report what needs to be fixed.
