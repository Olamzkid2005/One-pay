# Run Unit Tests

Run only unit tests (skip integration tests):

```bash
cd /Users/mac/Documents/One-pay && .venv/bin/python -m pytest tests/unit/ -v --tb=short
```

Summarize results: pass/fail count, duration, and any failures with root cause analysis.
