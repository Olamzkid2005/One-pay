# Generate API Key

Generate a new API key for the user. This script generates secure secrets:

```bash
cd /Users/mac/Documents/One-pay && .venv/bin/python generate_secrets.py
```

API keys are stored in the `api_keys` table and associated with user accounts.
