# Start Development Server

Start the Flask development server:

```bash
cd /Users/mac/Documents/One-pay && source .venv/bin/activate && python app.py
```

Report when server is running and provide the URL (typically http://localhost:5000).

Include these useful endpoints:
- Dashboard: http://localhost:5000/
- Health check: http://localhost:5000/health
- API docs: http://localhost:5000/api/v1/docs
